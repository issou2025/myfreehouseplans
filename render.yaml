# Render deployment configuration for MyFreeHousePlan
services:
  # Web Service
  - type: web
    name: myfreehouseplan
    runtime: python
    env: python
    pythonVersion: 3.11.9
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: FLASK_APP
        value: wsgi.py
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: PROTECTED_UPLOAD_FOLDER
        value: uploads_protected
      - key: UPLOAD_FOLDER
        value: app/static/uploads
      - key: DATABASE_URL
        fromDatabase:
          name: myfreehouseplan-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.9

  # Database (PostgreSQL)
  - type: pgsql
    name: myfreehouseplan-db
    region: oregon
    plan: free  # Change to starter or higher for production
    databaseName: myfreehouseplan
    databaseUser: myfreehouseplan_user
    ipAllowList: []  # Empty means allow all
